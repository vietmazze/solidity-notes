````
mkdir learn && cd learn

#create package.json
npm init -y

#install truffle
npx truffle init

#setup your version control
git init
git add
git commit -m "message"
git push -u main


#install truffle
npm install --save-dev truffle
````
